{
    "name": "Clientes_DynStock",
    "fields": {
        "CodigoCliente": "varchar(10)",
        "TipoEnvio": "varchar(20)",
        "FTP_Ip": "varchar(40)",
        "FTP_UserName": "varchar(25)",
        "FTP_Pass": "varchar(100)",
        "FTP_Path": "varchar(100)",
        "Email_Cliente": "varchar(200)",
        "FTP_port": "varchar(10)"
    },
    "queries": {
        "select": {
            "all": "SELECT * FROM Clientes_DynStock"
        }
    }
}
